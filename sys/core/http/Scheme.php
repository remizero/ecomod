<?php
namespace sys\core\http;

/**
 * <strong>Scheme</strong>
 *
 * Archivo creado el 24 de octubre de 2018 a las 08:46:30 a.m.
 * <p>Clase que define los protocolos a nivel de capa de aplicación (application layer).</p>
 *
 * @name Scheme
 * @namespace sys\api\config
 * @package ECOMOD.
 * @subpackage CORE.
 * @filesource Scheme.php
 * @version 1.0
 * @since 1.0
 * @author Filiberto Zaá Avila ( remizero ) filizaa@gmail.com.
 * @copyright Todos los derechos reservados 2018.
 * @link http://www.ecosoftware.com.ve
 * @license http://www.ecosoftware.com.ve/licencia
 * @uses <ul>
 *       <li>.php</li>
 *       </ul>
 * @see .php
 * @todo <p>En futuras versiones estarán disponibles los métodos para dar
 *       soporte a:</p>
 *       <ul>
 *       <li>http://diego.com.es/rendimiento-en-php.</li>
 *       <li>.</li>
 *       <li>.</li>
 *       </ul>
 */
class Scheme {

  const ABOUT = "about";
  const ACCT = "acct";
  const BGP = "bgp";
  const CRID = "crid";
  const DHCP = "dhcp";
  const DNS = "dns";
  const FILE = "file";
  const FTP = "ftp";
  const FTPS = "ftps";
  const GEO = "geo";
  const GOPHER = "gopher";
  const HTTP = "http";
  const HTTPS = "https";
  const IMAP = "imap";
  const INFO = "info";
  const LDAP = "ldap";
  const MAILTO = "mailto";
  const MGCP = "mgcp";
  const MQTT = "mmqtt";
  const NEWS = "news";
  const NFS = "nfs";
  const NNTP = "nntp";
  const NTP = "ntp";
  const POP = "pop";
  const ONC_RPC = "onc/rpc";
  const RTP = "rtp";
  const RTSP = "rtsp";
  const RIP = "rip";
  const SIP = "sip";
  const SIPS = "sips";
  const SMTP = "smtp";
  const SNMP = "snmp";
  const SSH = "ssh";
  const TAG = "tag";
  const TEL = "tel";
  const TELNET = "telnet";
  const TLS_SSL = "tls/ssl";
  const URN = "urn";
  const VIEW_SOURCE = "view-source";
  const WAIS = "wais";
  const WS = "ws";
  const WSS = "wss";
  const XMPP = "xmpp";
  
  // UNOFFICIAL SUPPORT
  const COFFEE = "coffee";
  const ED2K = "ed2k";
  const FEED = "feed";
  const FINGER = "finger";
  const IRC = "irc";
  const IRC6 = "irc6";
  const IRCS = "ircs";
  const LDAPS = "ldaps";
  const MAGNET = "magnet";
  const RSYNC = "rsync";
  const YMSGR = "ymsgr";
  
  
  

  // TODO - Insert your code here
  
  /**
   */
  public function __construct () {

    // TODO - Insert your code here
  }
}

